  <div class="container">
    <div class="row mt-5">
      <div class="col-9">
        <div class="mb-2">
          <h1><%= @demo.name %></h1>
        </div>
        <div class="demo-details">
          <p><i class="fas fa-map-marker-alt"></i> <%= current_user.first_name %> <%= current_user.last_name %></p>
          <p><i class="fas fa-calendar-alt"></i> <%= @demo.created_at %></p>
        </div>
      </div>
      <div class="col-3 mt-5">
        <div id="pitch">
          <h4>Pitch info here:</h4>
          <% if @pitches.blank? %>
            Add a New Pitch!
          <% else %>
            <%# @pitches.each do |pitch| %>
            <%# end %>
          <% end %>
        </div>
        <div id="personae">
          <h4>Personae info here:</h4>
          <% if @personas.blank? %>
            Add a New Persona!
          <% else %>
            <%# @personas.each do |persona| %>
            <%# end %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row mt-5">
      <div class="col-12">
        <%= link_to 'Take me to my Dashboard', dashboard_path %>
        <div class="m-4 text-center">
          <button type="button" class="btn">
            <%= link_to "Delete #{@demo.name}",
            demo_path(@demo),
            class: "btn btn-ghost btn-sm",
              method: :delete,
              data: { confirm: "Are you sure?" } %>
            </button>
          </div>
        </div>
      </div>
    </div>

